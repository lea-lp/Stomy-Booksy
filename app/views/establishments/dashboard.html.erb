<main class="main">

    <%= render 'dash-nav' %>

    <div class="content-inner content-docs">
        <div class="container-fluid">

            <!-- ACCUEIL -->
            <div class="pt-3 pb-4 mb-4 border-bottom">
                <div class="row">
                    <div class="col-lg-12">

                        <div id="dash-accueil">
                            <h4 class="text-danger text-center pb-4">..................................#1
                                ACCUEUIL................................</h2>
                                <%= render 'dash-accueil' %>
                        </div>

                    </div>
                </div>
            </div>

            <!-- RESERVATIONS -->
            <div class="pt-3 pb-4 mb-4 border-bottom">
                <div class="row">
                    <div class="col-lg-12">

                        <div id="dash-resa">
                            <h4 class="text-danger text-center pb-4">..................................#2
                                RESERVATIONS................................</h2>
                                <%= render 'dash-resa' %>
                        </div>

                    </div>
                </div>
            </div>

            <!-- PARTICIPANTS -->
            <div class="pt-3 pb-4 mb-4 border-bottom">
                <div class="row">
                    <div class="col-lg-12">

                        <div id="dash-particip">
                            <h4 class="text-danger text-center pb-4">..................................#3
                                PARTICIPANTS................................</h2>
                                <%= render 'dash-particip' %>
                        </div>

                    </div>
                </div>
            </div>

            <!-- PARAMETRES -->
            <div class="pt-3 pb-4 mb-4 border-bottom">
                <div class="row">
                    <div class="col-lg-12">

                        <div id="dash-param">
                            <h4 class="text-danger text-center pb-4">..................................#4
                                PARAMETRES................................</h2>
                                <%= render 'dash-param' %>
                        </div>

                    </div>
                </div>
            </div>

        </div>
    </div>

</main>